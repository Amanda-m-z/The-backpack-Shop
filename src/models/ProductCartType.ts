export type ProductCart = {   //Produkter i cart
    type: string;    //Typ av produkt
    price: number;   
    name: string;
    img: string;
    color: string;
    id: number;
    quantity: number;     
}